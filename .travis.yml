language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-index' ]; then cd .. && eval "mv $currentfolder generator-index" && cd generator-index; fi

